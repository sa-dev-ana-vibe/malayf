# Reviewer A — Screens + App shell

VERDICT: CHANGES REQUIRED

Findings:
  1. [blocking]      [§10] src/main.tsx:16-20 / src/App.tsx — no error boundary anywhere in the tree (confirmed: `grep -rn ErrorBoundary src/` → no match). Any render-time throw (a scoring edge case, `usePhotoUrl`, a malformed visit that slipped past a schema) blanks the entire app with no path forward. The role spec calls §10 out explicitly and §0/§10 of FRONTEND_QUALITY require "error boundaries at meaningful seams." The async edges are handled well (import → try/catch + alert at store.ts:484-488; photo migration → caught at store.ts:516-518), which makes the missing *render* boundary the one real resilience gap. Fix: wrap `<App/>` in main.tsx (and ideally `<DetailScreen/>` as a second seam, since it does the most per-render work) in an error boundary that shows a recoverable message ("Something broke — reload / export your data") instead of a white screen.

  2. [non-blocking] [§8] DetailScreen.tsx (ternary 616-633, stars 640-648/658, select 670-708), ApartmentsScreen.tsx:77-84, CompareScreen.tsx:75-82, DetailScreen tag/red-flag chips 408-414/445-451 — every selectable toggle is a `<button>` whose "selected" state is conveyed by color/weight only, with no `aria-pressed` (or `role="radio"`/`aria-checked` for the radio-like Pass/Fail/N-A and select rows). A screen-reader user can operate them but can't tell which answer/tag/filter is currently chosen. This is the largest a11y-beyond-the-linter gap; jsx-a11y won't catch it. Suggestion: add `aria-pressed={r === "pass"}` etc. to the toggle buttons, and consider a `radiogroup` for the mutually-exclusive ternary/select answer rows. The item text should also label its answer controls (e.g. wrap each answer group with `aria-label={text}` or `aria-labelledby`).

  3. [non-blocking] [§8] DetailScreen.tsx:39-49 (PhotoThumb floor-plan toggle) — in the non-floor-plan state the button's only content is the "▦" glyph and it relies solely on `title` for an accessible name; the floor-plan state renders "▦ ПЛАН" text. `title` is a weak accessible name and the toggle has no `aria-pressed`. Suggestion: add `aria-label={isFloorPlan ? "Убрать отметку планировки" : "Отметить как планировку"}` and `aria-pressed={isFloorPlan}` (the adjacent remove button at :50-56 already does the right thing).

  4. [non-blocking] [§8] App.tsx:33 / store.ts:92-97 — screen changes (tab→detail, detail→back, tags/redflags editors) swap whole sub-trees with no focus management. The element that was clicked unmounts and focus drops to `<body>`, so keyboard/SR users lose their place on every "route" transition. The role spec lists "handle focus on route transitions" under §8. Suggestion: on screen change, move focus to the new screen's heading/back button (e.g. a focus ref in `DetailScreen` top bar and in `Header`'s back button).

  5. [non-blocking] [§5] DetailScreen.tsx:61-741 — one ~680-line component renders ~11 unrelated sections (price, score, address, links, params, investments, tags, red flags, contacts, dates, photos, checklist, notes), each its own concern. It's a faithful 1:1 port of the prototype's single component and `PhotoThumb` is already extracted, so this isn't a correctness issue — but it's the one place the React rewrite could improve on the prototype by splitting the sections into presentational subcomponents (`AddressSection`, `ListingLinks`, `ParamsGrid`, `ChecklistSection`, …). Worth doing before this file grows further.

OK:
  - `npm run verify` GREEN (tsc -b + eslint + vite build, 57 modules, PWA precache generated). Machine gate clean; not re-reporting lint/type.
  - §3 effects: the only effects are DetailScreen's `document` paste listener (DetailScreen.tsx:66-82) and `usePhotoUrl` (ui/usePhotoUrl.ts) — both genuine external-system syncs, both correctly cleaned up; the paste handler reads live state via `activeVisit()` so the empty dep array is correct, not stale. No effect transforms data for render and none handles a user event. Clean.
  - §6 derive-don't-store: all scores (`scoreVisit`/`weightedScore`/`catScore`), `weightTotal` (ChecklistScreen:8), `itemCount` (BottomNav:8), sorting and tag-filtering (ApartmentsScreen:43-66) are computed during render. Nothing scored is stored. Clean.
  - §10 (the parts that exist): bad import → schema parse in try/catch with a user-facing message + confirm-before-replace (store.ts:481-504); photo migration failure caught and logged (store.ts:516-518); `pasteFromClipboard` degrades with an alert when the API is unavailable (store.ts:212-234). Destructive actions gated behind native `confirm()` (keyboard-accessible).
  - §10 empty states: ApartmentsScreen no-visits (19-34) and no-filter-match (119-123); CompareScreen no-visits (63-67), no-selection hint (86-90), and per-cell "—" placeholders (ui/styles infoCell/cellOf) — all present and matching the prototype.
  - §11 naming: domain-meaningful throughout — `redFlagDefs` vs a visit's `redFlags` (commented why, types.ts:64-67), `weightedScore`/`catScore`, `visibleVisits`/`sortedVisits`/`noFilterMatch`, `usePhotoUrl`. Comments explain *why* (e.g. the paste-effect rationale DetailScreen:64-65, the `pass`-aliases-`strong` note scoring.ts:81).
  - Helper reuse: screens consistently delegate to lib/scoring, lib/format, and ui/styles rather than hand-rolling — no duplicated formatting/scoring logic found in the screens.
  - Pixel/behavior fidelity to AptCheck.dc.html: verified faithful — titles map incl. checklist→"Settings" and tags→"Метки" (Header.tsx:5-12 vs proto:896), empty-state copy and the "appartment" typo preserved (proto:311-313), visits sort/filter/list region (proto:303-375), select-value display `'' / null → "0"` and `(без названия)`/`(unnamed item)`/`not rated` strings (scoring + DetailScreen vs proto:984-985,968,964), default select options `[100,0]` (store.ts:396-399 vs proto:826), `colorFor` thresholds (70/45) and compare `cellOf` color bands, floor-plan "▦ ПЛАН" badge, DELETE button, bottom-nav counts. No drift found.
